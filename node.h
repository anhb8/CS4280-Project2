#ifndef NODE_H
#define NODE_H

struct node
{
        char* data;
        struct node* left;
        struct node* root;
        struct node* right;
	char[] fName;
	struct token[] *arrToken;
};

#endif
